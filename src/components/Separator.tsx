import type { FC } from 'react';
import { styles } from '../utils/styles';

/**
 * Separator component for visual dividers between menu items.
 */
export const Separator: FC = () => <div className={styles.separator} />;
